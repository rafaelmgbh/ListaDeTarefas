<tr>
  <td><%= task.description %></td>
  <td><%= task.due_date%></td>
  <td><%= link_to 'Ver', task_path(task), class: 'btn btn-primary' %>
  <%= link_to 'Editar', edit_task_path(task), class: 'btn btn-warning' %>
  <%= link_to 'Excluir', task_path(task), method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-danger' %>
  </td>
</tr>